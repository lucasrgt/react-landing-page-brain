import React from "react";

const MethodBenefit = ({ title, description }) => {
  return (
    <div id="method-benefit">
      <p id="method-benefit-title">{title}</p>
      <p id="method-benefit-description">{description}</p>
    </div>
  );
};

export default MethodBenefit;
